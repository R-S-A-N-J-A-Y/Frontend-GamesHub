import HeroCard from "./HeroCard";

const HeroSection = () => {
  return (
    <div>
      <p className="fw-bold fs-2">Hello User !!</p>
      <HeroCard />
    </div>
  );
};

export default HeroSection;
